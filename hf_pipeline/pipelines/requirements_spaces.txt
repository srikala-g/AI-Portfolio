# Core dependencies for Hugging Face Spaces
gradio>=4.0.0
torch>=2.0.0
transformers>=4.30.0
diffusers>=0.20.0
datasets>=2.12.0
soundfile>=0.12.1
accelerate>=0.20.0
safetensors>=0.3.0

# Note: This configuration works on:
# - Hugging Face Spaces (CPU/CUDA)
# - Apple Silicon (M1/M2/M3/M4) with MPS
# - Local development with any PyTorch backend
